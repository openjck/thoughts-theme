{
  "name": "thoughts-theme",
  "private": true,
  "scripts": {
    "format": "prettier --write '**/*.css'",
    "lint": "prettier --check '**/*.css'",
    "build": "scripts/build"
  },
  "devDependencies": {
    "prettier": "^2.8.1"
  }
}
